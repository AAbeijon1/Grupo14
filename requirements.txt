Flask==3.1.1
Flask-SocketIO==5.5.1
ultralytics==8.3.147
opencv-python==4.11.0.86
numpy==2.2.6
torch==2.7.0
torchvision==0.22.0

# Other common dependencies that were observed during installations:
# These are often handled automatically when installing the main packages above,
# but listing some can be helpful for reproducibility if exact versions are needed later.
# Users can typically rely on pip to resolve these based on the primary package versions.

# Dependencies for Flask and Flask-SocketIO:
blinker>=1.9.0
click>=8.1.3
itsdangerous>=2.2.0
Jinja2>=3.1.2
MarkupSafe>=2.1.1
Werkzeug>=3.1.0
python-engineio>=4.11.0
python-socketio>=5.12.0
simple-websocket>=0.10.0
bidict>=0.21.0

# Dependencies for ultralytics (many are handled by torch/torchvision as well):
matplotlib>=3.3.0
Pillow>=7.1.2
PyYAML>=5.3.1
requests>=2.23.0
scipy>=1.4.1
tqdm>=4.64.0
psutil
py-cpuinfo
pandas>=1.1.4
ultralytics-thop>=2.0.0

# For production deployment consider:
# gunicorn
# eventlet or gevent (for Flask-SocketIO async mode)
